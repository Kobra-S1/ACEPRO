#####################################################################
#   Spoolman & ACE Integration for Anycubic Kobra 3 / S1 (Fixed)
#####################################################################

[gcode_macro SET_ACTIVE_SPOOL]
gcode:
  {% if params.ID and params.ID|int > 0 %}
    {action_call_remote_method("spoolman_set_active_spool", spool_id=params.ID|int)}
  {% else %}
    {action_call_remote_method("spoolman_set_active_spool", spool_id=None)}
  {% endif %}

[gcode_macro CLEAR_ACTIVE_SPOOL]
gcode:
  {action_call_remote_method("spoolman_set_active_spool", spool_id=None)}

[gcode_macro _SET_SPOOL_BY_TOOL]
gcode:
    {% set tool = params.TOOL|int %}
    {% set sv = printer.save_variables.variables %}
    
    {% set inv_key = 'ace_inventory_0' if tool < 4 else 'ace_inventory_1' %}
    {% set inv_idx = tool if tool < 4 else tool - 4 %}
    {% set hw_sku = "none" %}
    {% set hw_status = "unknown" %}
    
    {% if inv_key in sv %}
        # Defensives Stripping aller Quotes, um Parser-Fehler beim Vergleich zu vermeiden
        {% set hw_sku = sv[inv_key][inv_idx].sku|default("none")|string|trim|replace("'", "")|replace('"', "") %}
        {% set hw_status = sv[inv_key][inv_idx].status|default("unknown")|string %}
    {% endif %}

    {% set m_id = sv["manual_spool_id_" ~ tool]|default(0)|int %}
    {% set m_lock_sku = sv["manual_spool_lock_sku_" ~ tool]|default("none")|string|trim|replace("'", "")|replace('"', "") %}

    {% set rfid_id = 0 %}
    {% set sku_map = sv.sku_map|default({}) %}
    
    {% if hw_sku in sku_map %}
        {% set rfid_id = sku_map[hw_sku] %}
    {% elif hw_sku | int(0) > 0 %}
        {% set rfid_id = hw_sku | int %}
    {% endif %}

    {% if hw_status == "empty" %}
        CLEAR_ACTIVE_SPOOL
    {% elif m_id > 0 and hw_sku == m_lock_sku %}
        SET_ACTIVE_SPOOL ID={m_id}
        {action_respond_info("Spoolman: manually lock (ID %d) für Slot %d" % (m_id, tool))}
    {% elif rfid_id > 0 %}
        # Löst jetzt nur noch ein einziges Mal SAVE_VARIABLE aus, da Quote-Mismatch behoben ist
        {% if m_id > 0 and hw_sku != m_lock_sku %}
            SAVE_VARIABLE VARIABLE=manual_spool_id_{tool} VALUE=0
        {% endif %}
        SET_ACTIVE_SPOOL ID={rfid_id}
        {action_respond_info("Spoolman: RFID-ID %d loaded (Slot %d)" % (rfid_id, tool))}
    {% else %}
        CLEAR_ACTIVE_SPOOL
        {action_respond_info("Spoolman: No ID found for SKU '%s' (Slot %d)" % (hw_sku, tool))}
    {% endif %}

[gcode_macro MAP_SKU]
description: Verknuepft eine SKU dauerhaft mit einer Spoolman ID
gcode:
    # Bereinige die SKU von potenziell gefährlichen Quotes
    {% set sku = params.SKU|default("")|string|replace("'", "")|replace('"', "") %}
    {% set id = params.ID|default(0)|int %}
    
    {% if sku == "" or id == 0 %}
        {action_respond_info("Fehler: Bitte SKU und ID angeben.")}
    {% else %}
        {% set sv = printer.save_variables.variables %}
        {% set current_map = sv.sku_map|default({}) %}
        
        # Deep Copy des Dictionaries zur Verhinderung von In-Place Memory Mutations
        {% set new_map = {} %}
        {% for k, v in current_map.items() %}
            {% set _dummy = new_map.update({k: v}) %}
        {% endfor %}
        {% set _dummy = new_map.update({sku: id}) %}
        
        SAVE_VARIABLE VARIABLE=sku_map VALUE="{new_map}"
        {action_respond_info("Spoolman: SKU '%s' dauerhaft mit ID %d verknuepft." % (sku, id))}
    {% endif %}

[gcode_macro UNMAP_SKU]
description: Loescht eine verknuepfte SKU aus dem Speicher
gcode:
    {% set sku = params.SKU|default("")|string|replace("'", "")|replace('"', "") %}
    {% set sv = printer.save_variables.variables %}
    {% set current_map = sv.sku_map|default({}) %}
    
    {% if sku in current_map %}
        {% set new_map = {} %}
        {% for k, v in current_map.items() %}
            {% if k != sku %}
                {% set _dummy = new_map.update({k: v}) %}
            {% endif %}
        {% endfor %}
        SAVE_VARIABLE VARIABLE=sku_map VALUE="{new_map}"
        {action_respond_info("Spoolman: SKU '%s' aus dem Speicher geloescht." % sku)}
    {% else %}
        {action_respond_info("Spoolman: SKU '%s' nicht gefunden." % sku)}
    {% endif %}

[gcode_macro SPOOLMAN_MANUAL_SLOT]
description: Links a manual ID to the current spool in the slot
gcode:
    {% set slot = params.SLOT|default(-1)|int %}
    {% set id = params.ID|default(0)|int %}
    
    {% if slot >= 0 and slot <= 7 %}
        {% set sv = printer.save_variables.variables %}
        
        {% set inv_key = 'ace_inventory_0' if slot < 4 else 'ace_inventory_1' %}
        {% set inv_idx = slot if slot < 4 else slot - 4 %}
        {% set current_hw_sku = "none" %}
        
        {% if inv_key in sv %}
            {% set current_hw_sku = sv[inv_key][inv_idx].sku|default("none")|string|replace("'", "")|replace('"', "") %}
        {% endif %}

        SAVE_VARIABLE VARIABLE=manual_spool_id_{slot} VALUE={id}
        # Korrektes Escaping für den G-Code-Parser (schützt vor Spaces im String, iteriert sauber in ast.literal_eval)
        SAVE_VARIABLE VARIABLE=manual_spool_lock_sku_{slot} VALUE="\"{current_hw_sku}\""
        
        {action_respond_info("Spoolman: Slot %d manually locked to ID %d (base SKU: %s)" % (slot, id, current_hw_sku))}
    {% else %}
        {action_respond_info("Error: SLOT must be between 0 and 7.")}
    {% endif %}

#####################################################################
#   T-macros trigger tool changes Spoolman
#####################################################################

[gcode_macro T0]
gcode: 
  {% set p_len = params.PURGELENGTH|default(0) %}
  _SET_SPOOL_BY_TOOL TOOL=0
  ACE_CHANGE_TOOL TOOL=0 PURGELENGTH={p_len}

[gcode_macro T1]
gcode: 
  {% set p_len = params.PURGELENGTH|default(0) %}
  _SET_SPOOL_BY_TOOL TOOL=1
  ACE_CHANGE_TOOL TOOL=1 PURGELENGTH={p_len}

[gcode_macro T2]
gcode: 
  {% set p_len = params.PURGELENGTH|default(0) %}
  _SET_SPOOL_BY_TOOL TOOL=2
  ACE_CHANGE_TOOL TOOL=2 PURGELENGTH={p_len}

[gcode_macro T3]
gcode: 
  {% set p_len = params.PURGELENGTH|default(0) %}
  _SET_SPOOL_BY_TOOL TOOL=3
  ACE_CHANGE_TOOL TOOL=3 PURGELENGTH={p_len}

[gcode_macro T4]
gcode: 
  {% set p_len = params.PURGELENGTH|default(0) %}
  _SET_SPOOL_BY_TOOL TOOL=4
  ACE_CHANGE_TOOL TOOL=4 PURGELENGTH={p_len}

[gcode_macro T5]
gcode: 
  {% set p_len = params.PURGELENGTH|default(0) %}
  _SET_SPOOL_BY_TOOL TOOL=5
  ACE_CHANGE_TOOL TOOL=5 PURGELENGTH={p_len}

[gcode_macro T6]
gcode: 
  {% set p_len = params.PURGELENGTH|default(0) %}
  _SET_SPOOL_BY_TOOL TOOL=6
  ACE_CHANGE_TOOL TOOL=6 PURGELENGTH={p_len}

[gcode_macro T7]
gcode: 
  {% set p_len = params.PURGELENGTH|default(0) %}
  _SET_SPOOL_BY_TOOL TOOL=7
  ACE_CHANGE_TOOL TOOL=7 PURGELENGTH={p_len}